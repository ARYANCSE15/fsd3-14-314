#CRUD OPERATION
-C=Create
-R=Read
-U=Update
-D=Delete

#File system of Node JS
It allows Js code running outside the browser and interract directly to operating system

##Common operations on file/folder
-Reading and writing files->readFile(),writeFile(),appendFile()
-Directory management -> mkdir(),rmdir(),readdir()
-Metadata/information->stst(),istat(),fstat()
-Watching for changes->watch(),watchFile(),unwatchFile()
-Streaming Large Files->createReadStream(),createWriteStream()
-File Operation -> rename(),truncate(),unlink(),link(),syslink()
await can be used with any promise in a sunc function that a sync function will also accessed by await keyword